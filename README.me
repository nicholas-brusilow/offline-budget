# offline-budget

### By Nicholas Brusilow

*work in progress as of 21 April 2026*

An analysis tool for deep dives into personal spending and budget, designed for privacy-conscious individuals. It uses only .csv bank statments downloaded from the bank and does not connect to the internet.

### Instructions for Usage

So far, this only works for .csv file statements downloaded from Discover, Chase bank or Alliant Credit Union. I'm sure Claude Code can figure out how to make it work with your .csv bank statement though.

It's a bit funny how I have it set up. I actually run this in a jupyter scipy container, since that container can run python pretty well and you don't need to docker up and docker down every time you change something. It would be easy to modify the docker containerization to just run it with a python3-slim build.

Once you pull the repo, navigate to the root directory and run the command "docker compose up --build". This will build the jupyter container image. You can then launch jupyter by navigating to http://localhost:8888/lab in your browser.

Then, upload your .csv credit card and checking account statements into the appropriate folders in the "original_csv" directory.

Then, open the data_combining.ipynb notebook and run the cell there.

Finally, open the terminal in jupyter and run the command "python3 streamlit run src/main.py". It will then provide you a link that will open in your browser that lets you access the application.

To close it, just press CTRL+C in the Jupyter terminal, then run "docker compose down" in your machine's terminal.

### Explanation of Options

The "Period Scale" option in the table view is for scaling down once-yearly transactions. It is set to False by default. If, for example, you have a $3000 once-yearly home insurance payment, but are viewing that transaction in a one-month window, it will be scaled down to one-twelfth of its size in the graphs to accurately display how much was spent *for that period* in that category. This is so large, infrequent trasacttions don't skew and mess up the data visualization.

I'm still thinking about how this feature will interact with things like bi-yearly transactions.
